# while [ "${OPTIND}" -le "${#}" ]; do
#  opt="${!OPTIND}" # Prints the current string being processed
#  case ${opt} in   # Matches the positional params to the effect they need to do
#
#
#  esac
#  ((OPTIND++))
# done

imagemagick *.tif convert
